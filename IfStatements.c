#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool pizzaIsHealthy;
    int choice;
    printf("Do you think pizza is healthy? Type 1 for true and 0 for false: ");
    scanf("%d", &choice); //%d is preferred here 
    pizzaIdHealthy = choice;
    if(pizzaIsHealthy)
    {
        printf("Welcome to my Pizza App\n")
    }
}